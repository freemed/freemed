# $Id$
# $Author$

THISDIR=scripts
SUBDIR=fax_import gnupg quest_hl7

all:
	# Nothing

install:
	mkdir -p $(INSTDIR)/$(THISDIR)
	cp -vrf .ht* * $(INSTDIR)/$(THISDIR)

clean:
	# Nothing

dist-clean:
	# Nothing
	
